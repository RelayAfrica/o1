import express from 'express';import cors from 'cors';import {config} from './config';import {generalLimiter} from './middleware/rateLimiter';import {health} from './routes/health';import {business} from './routes/business';import {commerce} from './routes/commerce';import {push} from './routes/push';import {whatsapp} from './routes/whatsapp';import {delivery} from './routes/delivery';import {crm} from './routes/crm';import {social} from './routes/social';import {socialAdvanced} from './routes/social-advanced';import {campaigns} from './routes/campaigns';import {errorHandler} from './lib/errors';const app=express();app.use(cors({origin:config.CORS_ORIGIN}));app.use(express.json({verify:(req,_,buffer)=>{(req as import('express').Request).rawBody=Buffer.from(buffer)}}));app.use(generalLimiter);app.use('/health',health);app.use('/api/v1',commerce);app.use('/api/v1',push);app.use('/api/v1',whatsapp);app.use('/api/v1',delivery);app.use('/api/v1',crm);app.use('/api/v1',social);app.use('/api/v1',socialAdvanced);app.use('/api/v1',campaigns);app.use('/api/v1/businesses',business);app.use((_req,res)=>res.status(404).json({success:false,error:{code:'NOT_FOUND',message:'Route not found.'}}));app.use(errorHandler);app.listen(4000,()=>console.log('Relay API listening on :4000'));
