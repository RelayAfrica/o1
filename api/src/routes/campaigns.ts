import {Router} from 'express';import {z} from 'zod';import {requireRole,requireTenant} from '../middleware/auth';import {campaignAudienceCounts,sendCampaign} from '../services/campaigns';import {adminDb} from '../services/firebase-admin';export const campaigns=Router();campaigns.get('/businesses/:businessId/campaigns/push/audiences',requireTenant,async(req,res,next)=>{try{return res.json({success:true,data:await campaignAudienceCounts(req.businessId!)})}catch(e){return next(e)}});campaigns.get('/businesses/:businessId/campaigns/push',requireTenant,async(req,res,next)=>{try{const snap=await adminDb.collection('campaigns').where('businessId','==',req.businessId!).orderBy('createdAt','desc').limit(20).get();return res.json({success:true,data:snap.docs.map(doc=>doc.data())})}catch(e){return next(e)}});campaigns.post('/businesses/:businessId/campaigns/push/send',requireTenant,requireRole('owner','admin'),async(req,res,next)=>{try{const body=z.object({segment:z.enum(['all_opted_in','recent_purchasers','inactive']),title:z.string().min(1).max(80),body:z.string().min(1).max(240),deepLink:z.string().url().optional(),inactiveDays:z.number().int().positive().optional()}).parse(req.body);return res.status(201).json({success:true,data:await sendCampaign({...body,businessId:req.businessId!})})}catch(e){return next(e)}});
