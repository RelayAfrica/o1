import {Router} from 'express';import {z} from 'zod';import {requireRole,requireTenant} from '../middleware/auth';import {sendCampaign} from '../services/campaigns';export const campaigns=Router();campaigns.post('/businesses/:businessId/campaigns/push/send',requireTenant,requireRole('owner','admin'),async(req,res,next)=>{try{const body=z.object({segment:z.enum(['all_opted_in','recent_purchasers','inactive']),title:z.string().min(1).max(80),body:z.string().min(1).max(240),deepLink:z.string().optional(),inactiveDays:z.number().int().positive().optional()}).parse(req.body);return res.status(201).json({success:true,data:await sendCampaign({...body,businessId:req.businessId!})})}catch(e){return next(e)}});
