import {useNavigate} from 'react-router-dom';import {Button} from '../../components/ui/Button';export function GenericStep({step,title,copy,next,skip=false}:{step:number;title:string;copy:string;next:string;skip?:boolean}){const nav=useNavigate();return <section><div className="eyebrow">Step {step} of 5</div><h1>{title}</h1><p className="step-intro">{copy}</p><div className="card"><p className="muted">You can update this anytime from your workspace settings.</p><div className="field"><label>{step===2?'Brand color':step===3?'Store URL':'First product name'}</label><input placeholder={step===2?'#3BB741':step===3?'your-business':'Add something customers can buy'}/></div></div><div className="step-actions">{skip?<button className="btn btn-ghost" onClick={()=>nav(next)}>Skip for now</button>:<span/>}<Button onClick={()=>nav(next)}>Continue →</Button></div></section>}

