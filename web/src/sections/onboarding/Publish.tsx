import {useNavigate} from 'react-router-dom';import {Button} from '../../components/ui/Button';export function Publish(){const nav=useNavigate();const data=JSON.parse(localStorage.getItem('relay.business')??'{}') as {name?:string};return <section><div className="eyebrow">Step 5 of 5</div><h1>You’re ready to launch</h1><p className="step-intro">Here’s your Relay workspace. You can keep refining it as you grow.</p><div className="card"><div className="eyebrow">Business</div><h2>{data.name||'Your business'}</h2><p className="muted">Your store is set up as a draft and ready to publish.</p></div><div className="step-actions"><button className="btn btn-ghost" onClick={()=>nav('/dashboard')}>Finish setup later</button><Button onClick={()=>{localStorage.setItem('relay.active','true');nav('/dashboard')}}>Launch my store →</Button></div></section>}

