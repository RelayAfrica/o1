import {Link,useNavigate} from 'react-router-dom';import {useState} from 'react';import {signInWithEmailAndPassword} from 'firebase/auth';import {auth,firebaseConfigured} from '../../lib/firebase';import {Button} from '../../components/ui/Button';import {Input} from '../../components/ui/Input';
export function SignIn(){const nav=useNavigate();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const submit=async(e:React.FormEvent)=>{e.preventDefault();setError('');if(!firebaseConfigured||!auth){nav('/onboarding');return}try{await signInWithEmailAndPassword(auth,email,password);nav('/dashboard')}catch(err){setError(err instanceof Error?err.message:'Unable to sign in')}};return <form className="auth-form" onSubmit={submit}><div className="eyebrow">Welcome back</div><h1>Sign in to Relay</h1><p className="muted">Run your business with less friction.</p><div className="field"><label>Email</label><Input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></div><div className="field"><label>Password</label><Input type="password" required value={password} onChange={e=>setPassword(e.target.value)}/></div>{error&&<p className="error">{error}</p>}<div className="actions"><Button type="submit">Sign in</Button></div><p className="auth-link">New to Relay? <Link to="/sign-up">Create an account</Link></p></form>}

