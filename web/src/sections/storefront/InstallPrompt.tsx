import {useInstallPrompt} from '../../hooks/useInstallPrompt';export function InstallPrompt(){const {canInstall,ios,install}=useInstallPrompt();if(!canInstall&&!ios)return null;return <aside className="install-prompt"><strong>Keep this store handy</strong>{ios?<p>Tap Share, then “Add to Home Screen” to install it.</p>:<button onClick={install}>Add to Home Screen</button>}</aside>}
