{
  "name": "relay",
  "private": true,
  "scripts": {"dev": "concurrently \"npm --prefix web run dev\" \"npm --prefix api run dev\"", "build": "npm --prefix web run build && npm --prefix api run build", "test": "npm --prefix api run test"},
  "devDependencies": {"concurrently": "^9.1.2"}
}

