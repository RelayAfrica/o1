{
  "name": "relay",
  "private": true,
  "scripts": {"dev": "concurrently \"cd web && npm run dev\" \"cd api && npm run dev\"", "build": "cd web && npm run build && cd .. && cd api && npm run build", "test": "cd api && npm run test"},
  "devDependencies": {"concurrently": "^9.1.2"}
}
